apiVersion: apps/v1
kind: Deployment
metadata:
  name: cyberguard-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cyberguard-redis
  template:
    metadata:
      labels:
        app: cyberguard-redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: cyberguard-redis
spec:
  ports:
  - port: 6379
    targetPort: 6379
  selector:
    app: cyberguard-redis
